apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: wso2is-storage
provisioner: kubernetes.io/no-provisioner
volumeBindingMode: WaitForFirstConsumer

---

kind: PersistentVolume
apiVersion: v1
metadata:
  name: wso2is-volume
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: wso2is-storage
  hostPath:
    path: "/Users/Shared/Kubernetes/persistent-volumes/wso2is"